\section{The Data-Capturing Process}
\label{sec:dataset:process}

Now that we have defined \textit{what} to mark up, we now describe the process to define \textit{how} we capture it. At its core, this is an \gls{etl} process. \todo{Include activity diagram. Consider readjusting for metamodel .}

% Talk about automatic mappings. e.g. drag and drop, UI generation with descriptive labels etc. Things we can make the UI automate
% Then discuss provenance and how we can track using git
